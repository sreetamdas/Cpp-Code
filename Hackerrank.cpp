#include<iostream>
#include<string>
using namespace std;
int main()
{
	int i,len, j;
	char str[100], m, 
	cout<<"Enter the string\n";
	gets(str);
	for(i=0;str[i]!='\0';i++)
	{}
	cout<<"Length of string = "<<i;
	len=i;
	int count[len];
	char STR[len];
	for(i=0;i<len;i++)
	{
		count[i]=-1;
		for(j=0;j<len;j++)
		{
			if(str[i]==str[j])
				count[i]++;
		}
		
	}
	for(i=0,j=len;i<len;i+++,j--)
	{
		for
	
	
	
	
	return 0;
	
}
